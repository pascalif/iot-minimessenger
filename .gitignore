# Arduino IDE build artifacts (normalement dans /tmp, on couvre par sécurité)
build/
*.elf
*.bin
*.hex
*.map
*.o
*.d

# Editor / OS noise
.vscode/
.idea/
*.swp
*~
.DS_Store

# Claude Code — local/personal settings (mémoire versionnée, mais pas les overrides locaux)
.claude/settings.local.json
.claude/*.local.*
